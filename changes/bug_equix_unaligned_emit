  o Minor bugfixes (C correctness):
    - Write the immediates that hashx's code generators emit with memcpy()
      rather than through a pointer of the immediate's own type. x86
      instructions vary in length, so the destination is rarely aligned,
      and the store was undefined behaviour. Found by
      UndefinedBehaviorSanitizer. Fixes bug 0000; bugfix on 0.4.8.1-alpha.
